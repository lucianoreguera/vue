import { defineConfig } from 'vitest/config'

export default defineConfig({
    test: {
        // exclude: excluir directorio de test
        // globals: api globales -> y mas: ver documentacion de vitest
    },
    // plugins
})